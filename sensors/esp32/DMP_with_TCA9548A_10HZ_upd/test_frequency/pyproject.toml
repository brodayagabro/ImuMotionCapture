[project]
name = "frequency-analysis"
version = "0.1.0"
description = "Analyze measured UDP packet arrival frequency"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.10,<4",
]

[tool.uv]
package = false
